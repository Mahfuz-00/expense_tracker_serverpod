name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.4.7'
      SERVERPOD_PASSWORD_database: 'AfcjO4gTMpVk_JRfWqZ-GlUh5_WG9utV'
      SERVERPOD_PASSWORD_redis: 'hlABqjcyrRH8-0aQoJMITMCam1ZSDFnP'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: expense_app_admin_server
        run: docker compose up --quiet-pull --quiet-build --build -d

      - name: Install dependencies
        working-directory: expense_app_admin_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: expense_app_admin_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: expense_app_admin_server
        run: serverpod generate

      - name: Run tests
        working-directory: expense_app_admin_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: expense_app_admin_server
        run: docker compose down -v
